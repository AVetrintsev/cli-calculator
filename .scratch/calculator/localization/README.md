# Interface text and translations

This directory is a specification asset. It contains candidate copy for the currently identified product surfaces; there is no application implementation yet. A catalog entry does not approve a feature, a default setting, a keyboard binding, or a window layout. Entries whose context describes an undecided flow must be reviewed when that product decision is resolved.

The catalog and both language files are siblings:

- [catalog.json](catalog.json): the English source text and English translation guidance for every message.
- [en.json](en.json): the English message values, indexed by stable message keys.
- [ru.json](ru.json): the Russian message values, indexed by the same keys.

Localized mathematical unit symbols have their own sibling [unit-symbol catalog and locale files](units/README.md). Their active vocabulary contains only the Latin baseline plus one selected local language. They are parsed tokens with stable unit identities; ordinary interface messages remain in the files above.

## Catalog structure

The catalog has a schema version, a source language, a draft status, a coverage note, and a `messages` object. Each property in `messages` is a stable, descriptive key such as `copy.success`.

Each message has five required fields, all written in English:

| Field | Purpose |
| --- | --- |
| `text` | The full English source text, including named placeholders. |
| `meaning` | What the text communicates to the user; disambiguates short or repeated phrases. |
| `context` | The state, event, or user action that makes the message relevant. Unresolved behavior is identified explicitly. |
| `location` | The semantic region in which the text appears or is announced. Exact geometry is not assumed before the interface prototype is agreed. |
| `elementType` | The kind of element that uses the text, including text that is only available to assistive technology. |

Messages with inserted values also have a `placeholders` object. It describes each named value in English. These descriptions are translation guidance, not text displayed to the user.

Current element types: `input_placeholder`, `accessible_name`, `accessible_description`, `field_label`, `result_value`, `status_message`, `command_label`, `input_hint`, `error_message`, `section_heading`, `empty_state`, `dialog_title`, `select_option`, `description`, `menu_item`, and `button`.

## Consistency rules

1. The catalog and each complete language file must contain exactly the same message keys. Keys remain stable when wording changes.
2. Each `en.json` value must equal its catalog entry's `text` exactly. Update them together; the catalog is the authoring reference for English wording and translation context.
3. Each translation must be a nonempty string and retain the same named placeholders. A translator can reorder complete placeholders to fit the target language.
4. A translated sentence must be stored as a whole. Do not assemble user-facing sentences from independently translated fragments or add hard-coded English punctuation at call sites.
5. User-entered expressions, variable names, saved constant and function names or bodies, template names, file contents, and calculated values are data, not interface text. Do not translate or rewrite them through this catalog. Numeric formatting and the reduced opacity of fractional digits belong to result presentation, not translation strings.
6. Keyboard combinations are supplied in their platform-specific form through `{shortcut}`. Do not bake a Windows modifier into wording shared with macOS.
7. Use separate keys when identical English words have different meanings or translation requirements. Reuse a key only when its meaning and context are also shared.
8. A new label, placeholder, error, hint, menu item, dialog message, or accessible name must be added to the catalog and both initial language files before that application text is implemented.

## Adding another language

1. Read the full catalog so that meaning, execution context, location, and element type inform the translation.
2. Generate a sibling language JSON file, such as `de.json`, containing the same flat mapping from message keys to translated strings.
3. Preserve the keys and placeholder names. Translate values only; do not copy the metadata into the language file.
4. Validate key coverage, nonempty strings, and placeholder parity against the catalog. Preserve UTF-8 encoding.
5. Review meaning and terminology, then check the translation in the actual interface for clipping, text expansion, keyboard access, and screen-reader announcements.

Generation from the catalog provides a translation draft. Structural validation cannot certify its linguistic quality or visual fit.

## Coverage and remaining decisions

The initial catalog covers quick input, live results and diagnostics, history, calculation sheets, commands for saving user constants and functions, templates, settings, local data transfer, application menu commands, and shared dialog actions. Candidate text for optional interactions is marked as such in each message's context. There are no visible standard window controls added to the quick calculator by this catalog. The former calculation-wait message was removed after the long-running calculation flow was excluded.

Installer and updater wording, additional mathematical diagnostics, new built-in templates, and the final texts for unresolved interaction flows must be added as those parts of the specification become concrete. System-owned dialog text is supplied by the operating system; application-supplied titles or messages still belong in the catalog.

Numeral-system conversion targets bin, oct, dec and hex are invariant language tokens, not physical units or translated labels. Their completion descriptions belong to this interface catalog. Suggestions open immediately after a complete :: or to operator. Integer values without a unit are offered numeral-system targets; quantities with units are offered compatible units. Both operators support the same conversion kinds but have different precedence: :: binds before multiplication and addition, while to applies after arithmetic. The source operand used to select suggestions must follow that grouping. Diagnostics cover fractional nondecimal conversion, incompatible unit-bearing values, invalid prefixed digits and the shared integer-magnitude limit. An incomplete prefix retains the agreed empty-result behavior. Decimal precision and integer magnitude use distinct diagnostics: more than 30 binary digits is not itself an error. Only decimal digits are grouped with spaces; other bases remain contiguous in input and displayed results.

Operator-help fragments use stable help.precedence.* keys with English meaning, context, location and element-type guidance. The [help materials](../help/README.md) define their order and generate the English and Russian Markdown previews from these locale values. The full precedence table and whitespace rule were approved in expression-language round 20; round 21 adds comparison chains and distinguishes superscript unit notation from caret exponentiation of a quantity. Separate operand and boundary-case contracts remain open. Contextual lowercase m/м is declared separately in the [unit-symbol specification](units/README.md); the user selected showing all valid answers when an expression has multiple interpretations and unambiguous metre square/cube shorthand. Explicit metre/min aliases and duration sums are approved. Square/cube shorthand formatting now applies to all known length symbols and explicit length aliases in the Latin baseline and active locale. Only whole symbols are formatted: m20/m2rate are not rewritten by prefix, and continuing a word removes superscript formatting. Backspace after m² deletes its exponent; formatting adds no separate undo step. Pasted formulas receive the same formatting in one undo step; selected formula text copies the visible superscript characters. Quick numeric result copy preserves displayed numeric text without its unit; other copy commands and persistence remain open. Identical-looking formatted answers are displayed once, retaining their underlying meanings and exact values. The updated variant A has a shared horizontal row with thin vertical dividers, horizontal overflow and no interpretation captions. The result.alternative.interpretation message was removed; descriptions in autocomplete remain. Simple compound units are limited to speed, pace and information-transfer rate, while area and volume remain supported. Support is checked after final unit cancellation; interpretations with unsupported final units are discarded while remaining valid answers are shown. Compact durations such as 1h30m are accepted. Components may be reordered and repeated: 30m1h gives 90 minutes, and 1h30m15m gives 105 minutes. The ambiguous 1m30m / 1m 30m retains two readings, a duration sum of 31 minutes and a length product of 30 square metres; this does not approve addition of adjacent lengths. Compound durations form one value under surrounding operators; 1h30m * 2 gives 180 minutes and -1h30m gives minus 90 minutes. An incomplete 1h30 has no result. A following quantity of a clearly different dimension requires an operator; 1h30m2km uses the candidate error.missingOperator text. This does not prohibit other accepted implicit multiplication. The min and max functions require two or more arguments. The min spelling followed by one complete parenthesized expression denotes minutes multiplied by that expression; without a coefficient, one minute is implied. Nested separators do not count as outer arguments. The incomplete 2min(2; has a blank result; the closed 2min(2;) uses error.missingArgument and cannot become a minute product. Other malformed lists, other error policies, copy selection and remaining editing behavior stay open. Help content uses help_heading, help_paragraph and help_table_row element types in addition to the UI types above.

Round 32 accepts system-language selection when a translation exists, English-only fallback otherwise, immediate language switching and independently configured number formatting with a system default. A missing individual message uses its English source. Only the Latin baseline plus the resolved active UI locale supply input aliases; an unavailable locale is not activated. Recognized built-in symbols adapt semantically, while user names, comments and incomplete fragments remain intact. Calendar formatting follows the calendar contract. The runtime localization library, plural handling and right-to-left layout remain architecture decisions. `{name}` is a placeholder notation for these assets, not a choice of runtime interpolation library. Count-based messages must not be introduced with English-only plural assumptions.

Product decision: [Product scope and usage model](../issues/01-product-scope.md).

The user requested quick result copy: Ctrl+C in the editor with no selection and a single ready displayed result entry, plus clicking the target result. Text selection retains ordinary text copying. The help.copy.quickRule fragment uses {shortcut}: Ctrl+C on Windows and Cmd+C on macOS are accepted. Copy success/error messages must reflect the actual clipboard outcome. Quick numeric copy omits units while preserving displayed numeric rounding, grouping and numeral-system notation; formula selections retain unit text. It keeps the window and input, restores input focus after a click and does not add history. A single displayed entry permits shortcut copy without choosing a hidden interpretation. With editor focus, multiple entries without text selection preserve the clipboard and show copy.chooseResult. Keyboard selection for confirmation was accepted in copying round 6 and is documented in help.confirm.multipleRule. The editor copy shortcut keeps its existing conditions; other remaining rules are tracked in the [copying question](../issues/06-history-and-copy.md). This does not add result captions or a new settings toggle.

The accepted default Enter confirmation in quick mode has its own help.confirm.quickRule fragment, separate from help.copy.quickRule. With one ready displayed result, confirmation records history, copies according to the same payload rules and immediately focuses a new empty input in the same location while keeping the window open. Ordinary quick-copy shortcuts and clicks keep the current formula and create no history entry. Copying round 4 also accepts Ctrl+Enter on Windows / Cmd+Enter on macOS to confirm and hide after full success, with empty input on the next invocation; help.confirm.hideRule documents this. Failure to save history or copy preserves the formula, result and open window and shows the appropriate brief error. history.save.error is editorial wording for a history-write failure; copy.error remains specific to clipboard failure. Confirmation success must not be inferred merely from a successful clipboard write. Copying round 5 accepts retrying incomplete confirmation without duplicating an already saved history entry while the formula and calculation are unchanged; help.confirm.retryRule documents it. After full success, use the command invoked for the retry. Successful Enter also collapses expanded history; Arrow Up opens it again with the newest entry nearest the stationary input. Failed confirmation does not collapse history. Operation order and recovery after restart remain architecture decisions; no atomic transaction with the system clipboard is promised. These messages add no visible calculator buttons.

Copying round 6 adds help.confirm.multipleRule with platform shortcut placeholders: initial Enter or modified-Enter focuses the existing result row without copying or recording; Left/Right choose and scroll; the next confirmation command records and copies the chosen answer, then starts empty input with history collapsed or hides. Escape restores the formula cursor without changes, and clicks remain copy-only. The first highlighted entry is navigation focus, not automatic confirmation. result.alternatives.label remains an accessible collection name without visible interpretation captions. Copying round 7 accepts the stored formula, complete result set and confirmation choice, with historical values preserved and recalled input recalculated using current definitions; help.history.recordRule documents this. Copying round 8 accepts a row with the formula and all saved displayed results, bold emphasis on the confirmation choice and horizontal overflow; exact serialization, visual styling and accessibility verification remain separate work.

The help.history.recordRule fragment uses the same English metadata and paired locale files to explain stored history and recalculation. History keeps all working values and hidden interpretations, not just the rounded unitless clipboard text; a later definition change does not rewrite the old results. Recalling the formula evaluates a new copy using current definitions. history.entry.accessibleName now uses {expression} and {results} for the complete saved displayed result set; the editorial history.result.chosen description identifies the answer chosen at confirmation without adding visible captions. Native list and selection announcements still require verification and must not substitute live recalculations for historical results.

Copying round 8 adds help.history.displayRule and extends help.history.recordRule: rows show the formula and all saved displayed answers, emphasize the confirmation choice in bold and scroll horizontally. Each new successful confirmation creates a separate record even for identical content; unfinished-confirmation retry remains protected against a duplicate. The new accessible_description element type is programmatic text, not a visible interpretation label. Copying round 9 accepts history search, single-record deletion with Undo and confirmation before clearing history; their remaining interaction and recovery details are tracked in the history question.

Copying round 9 adds help.history.searchRule with a platform shortcut parameter and help.history.manageRule. Search targets saved formulas and results without recalculation, preserving the current formula and cursor in the fixed input. Copying round 10 extends history.undo to deletions in reverse order and whole-history clearing as one operation until full application exit; hiding the window preserves availability and newer calculations are kept on restoration. history.clear.title, history.clear.description and history.clear.confirm support the accepted whole-history confirmation, with common.cancel available to dismiss it. The editorial clearing text now explains the accepted Undo availability until full application exit, without promising it after restart. Both deletion and clearing preserve constants, functions, sheets and templates.

Copying round 10 updates search help and metadata: filter as the query is typed, choose matches with Up/Down without changing the formula, use Enter only to recall the selected formula for recalculation, or Escape to restore the original draft and cursor. With no matches Enter recalls nothing. Copying round 11 accepts case-insensitive substring matching in formulas and any saved displayed answer, normal chronological order and the entire history for an empty query; copying round 12 adds the accepted number-format and unit-power spelling equivalents described in help.history.searchNotationRule. History Undo restores complete deleted records, including a full clear as one operation, without rolling back calculations created later. It remains separate from formula text undo.

Copying round 11 adds help.history.retentionRule: the entire history stays saved between launches until manual deletion, without age or record-count cleanup. Its lifetime is separate from session-limited deletion Undo. Search help now explains case-insensitive substring matching, preserved history order and an empty query showing all records. Copying round 12 accepts decimal separator/grouping and known unit superscript equivalents; copying round 13 adds the shared history menu described in help.history.menuRule; the search still does not recalculate records or change case-sensitive expression semantics.

The help.history.searchNotationRule fragment documents copying round 12 with English metadata and paired translations. Additional matches normalize recognized decimal separators and grouping and known whole length-unit square/cube spellings, while keeping ordinary text matches. Caret exponentiation remains distinct from unit shorthand; this does not prevent a history record from matching one of its saved displayed answers. No formula evaluation, base/unit conversion or rewriting of names, comments or stored values is introduced.

Copying round 13 places history.reuse, history.delete, history.undo and history.clear in one menu; their element type is menu_item. The approved Russian labels are paired with English labels Use formula, Delete entry, Undo and Clear history. history.menu.accessibleName is programmatic text, not a visible title. help.history.menuRule uses a platform shortcut parameter for Ctrl+Shift+H on Windows / Cmd+Shift+H on macOS inside quick mode and documents right-click access. Opening preserves the formula; Escape restores previous focus. Menu placement and native behavior still need verification. Copying from an already focused result row remains the next separate proposal, rather than a change to the editor copy shortcut.

Copying round 14 adds help.copy.focusedResultRule with {shortcut}: Ctrl+C/Cmd+C with focus in the current result row copies its highlighted ready answer and keeps focus and selection there. An inactive selection retained in the formula editor does not override this target. help.copy.quickRule explicitly describes editor focus; its multiple-answer hint is not a restriction on row-focus copying. help.confirm.multipleRule also documents returning to ordinary editing at the prior cursor/selection on typing, paste, Backspace or Delete, ending answer selection and recalculating without copying or confirmation. These fragments do not assign copying or editing commands in history, search, menus or sheets. Native focus behavior remains to be verified.

Copying round 15 updates help.confirm.multipleRule: Left/Right stop at the current result row boundaries without wrapping; Up/Down end selection and perform ordinary history navigation on the same keypress. The new help.history.copyRule fragment describes clicking a saved historical answer to copy its displayed value without recalculation, preserving the current formula/cursor and returning focus to input. Numeric payloads omit units under the existing formatting rules; changed definitions do not invalidate historical values. The clicked target need not be the answer chosen at confirmation, and that stored choice remains unchanged. Keyboard copy targeting within history and sheet behavior remain separate decisions. These English metadata and paired en/ru texts add no visible buttons or result captions.

Copying round 16 adds help.history.navigationRule for direct Up/Down recall and return to the original input, help.input.undoRule for recall undo and confirmation/session boundaries, help.quick.restoreRule for normal-restart draft restoration, help.results.orderRule for fixed metre-before-minute ordering, and help.history.extendedShortcutRule for Alt+Up using either modifier. The user replaced the proposed Tab history-view mode; it was not accepted. The previous unconditional empty return and Ctrl+Up history-access assignment are superseded. The retry fragment now explicitly treats confirmation of a different answer after partial failure as a new entry while preserving an already saved earlier choice. These fragments have complete English translation metadata and paired en/ru texts; they remain outside the operator-precedence preview. History insertion into a calculation sheet and automatic collapse after return to nonempty original text are pending decisions, not approved behavior.

Round 17 is partially accepted: point 1 updates help.history.navigationRule and quick.history.hide for collapse on return to any original input and navigation boundaries; point 2 expands help.history.extendedShortcutRule for history focus, saved-answer copy and one-edit formula insertion in a sheet; point 3 adds help.sheet.dependenciesRule and editorial duplicate/dependency diagnostics. The model is scoped to sheet-local definitions and preserves sequential user-function bodies. Points 4-6 remain pending after the user asked about scope: per-line result display, sheet-local shadowing of saved constants and named-sheet autosave are not accepted by that question. These messages do not change global constant/function persistence. Interaction and sheet-model fragments remain outside the operator-precedence preview.

Round 17 is now fully accepted after the user confirmed points 4-6. help.sheet.resultsRule, help.sheet.scopeRule and help.sheet.autosaveRule describe per-expression sheet results, sheet-local shadowing of saved constants and local autosave of named sheets. Sequential function-body behavior and explicit saving of global definitions remain distinct. sheet.save is an unassigned candidate label, not a required manual save control; sheet.saved and sheet.saveError reflect actual persistence outcomes even for text that cannot calculate. The new help fragments include English translation metadata and matching en/ru texts and remain outside the operator-precedence preview. Round-18 proposals about copying, error propagation and the global library have not been turned into accepted help.

Round 18 is fully accepted. help.sheet.copyRule and help.sheet.errorRule specify current-expression copy, sheet-row focus, no history/confirmation reset and independent calculation around invalid dependencies. The six help.definitions fragments cover function scope, live constant formulas, explicit draft publication, name conflicts, semantic rename and deletion with separate session-limited Undo. Rename-aware history recall preserves original historical text and values; ordinary selected-text copy stays literal. English metadata and matching en/ru translations include definition commands and diagnostics, while exact management placement and native verification remain open. The accepted behavior does not publish global drafts through sheet autosave. These fragments remain outside the operator-precedence preview. Round 19 about document management, templates and transfer is pending in its decision ticket and has not been published as accepted behavior here.

Round 19 accepts the shared library, template-to-independent-sheet lifecycle, document management and session-limited deletion Undo, portable transfer, import preview/conflict copies and local backups. The user correction excludes history from full export; local history remains retained, and backup contents are not inferred from export scope. New library, template, document and data help uses complete English translation metadata with matching en/ru entries. Existing template-only search labels are unassigned candidates because the accepted search is shared. The seven new help fragments stay outside the operator-precedence preview. Round 20 keeps parameter-entry details, common-settings application, backup composition/restore semantics, storage-failure recovery and restored-document dependencies as pending proposals.

Round 20 is fully accepted. Parameter help now describes ordinary expressions in compact prefilled fields and validation before sheet creation. Import settings are an unchecked-by-default opt-in. Local backups include history and settings; explicit full-state restoration previews additions, changes and removals and requires a successful current-state backup first. Portable export remains history-free. Save-failure and document-restore help distinguish retained draft text, explicit exit choices, recovery of the last durable state and missing semantic dependencies without global-library rollback. New commands, checkbox, status and diagnostic text have English metadata and matching en/ru entries. These five added help fragments remain outside the operator-precedence preview. Financial-model proposals and research are not accepted calculator behavior or active financial notation.

Round 21 is fully accepted. Seven financial help fragments describe rate/period conventions, loans, savings, whole-period profit/ROI, the two payback models, shared precision and explicit user-supplied conditions. Nine completion description sources and the no-payback/zero-investment-ROI diagnostics have full English translation metadata and paired en/ru text. The descriptions refer to accepted models; they do not register candidate function identifiers or localized aliases. Round 22 keeps names, periodic suffixes, signatures, timing notation, proposed annual-rate conversion helpers, input bounds and percentage copying pending. Financial help remains outside the operator-precedence preview, and no runtime function registry has been created.

Round 22 accepts financial identifiers, positional signatures, timing spellings, annual-rate conversions and input boundaries. The [financial notation catalog](functions/README.md) has 16 function identities and 2 timing symbols with English metadata and matching en/ru names. The [signature contract](../language/financial-functions.json) binds models, defaults and localized parameter labels; it is specification data, not a runtime implementation. New help and completion text follows the same translation metadata requirements.

The user explicitly corrects percentage copy: displayed 30% copies the decimal fraction 0.3 or 0,3 according to number-format locale. Main result-copy help includes that exception across current results, confirmation, sheets and saved history, without changing selected-text copying or historical evaluation. General timing-value behavior, long-percentage clipboard precision and uniform display of percentage literals are still round-23 proposals. No pending proposal is registered as active help or notation. These additions remain outside the operator-precedence preview.

Round 23 is accepted in full. Percentage results now display with %, while their clipboard payload is the displayed coefficient divided exactly by 100 without hidden digits or another rounding step. Timing values can be stored and passed through user definitions; their standalone output copies the localized symbol. The [logical value catalog](values/README.md) registers true/false and active-locale да/нет separately from Yes/No output labels. Help also covers correlated meanings of a named value, branch-local numerical failure filtering, and whole-expression syntax/name validation with lazy numerical conditions. These new help fragments remain outside the operator-precedence preview.

The user also adds dates, time and dynamic today/yesterday values to product scope. Exact calendar notation, clock refresh and copying rules are pending in [the calendar decision](../issues/19-calendar-and-clock.md); they are not registered as active spellings or behavior merely from examples in that ticket. This replaces the former general calendar exclusion without changing the financial-period conventions.

Round 24 is fully accepted. Four dynamic names join the [value catalog](values/README.md); [calendar input pattern files](calendar/README.md) contain English metadata, the universal YYYY-MM-DD baseline and active-ru DD.MM.YYYY. New help and diagnostics cover accepted date/time input, one shared clock snapshot, live refresh, basic arithmetic, calendar month overflow, fixed offsets without seasonal transitions, and full calendar-result copying. Dates and times are primary literals in the operator table, with a new explanation of date recognition versus spaced subtraction; both operator previews are regenerated from the same fragments.

The [calendar contract](../language/calendar.json) remains specification material. Round-25 proposals about interval/offset notation, compound periods, mixed temporal values, finer computed precision and partial confirmation retry are not registered as accepted help or aliases. Numeric pattern tokens Y/M/D are not words to freely translate. Fixed-offset notation is separate from the accepted support for UTC and offsets.

Round 29 and its subsequent amendment are recorded. Help now uses the user's binding scale examples: 1m + 1m = 2m, 1m + 100mm = 1100mm and 1m + 100mm to m = 1.1m. Earlier unconditional floor/duration automatic-scale examples are under review; reference-rounding help uses an explicit target to avoid presenting unsettled behavior as decided. Scale candidates refer to the existing unit IDs, not translated strings. No round-30 proposals are registered as active messages or aliases.

Round 30 accepts scale ranking and its common scope, plus explicit unit-target grammar. The active help now states floor(1234mm) = 1000mm and preserves single-scale durations. The operator preview gains the unit-target rule from the same English metadata/en/ru catalog. A localized caret-target diagnostic uses the corrected unit token as a placeholder. Unit-completion help records prefix continuations and operation-sensitive local filtering; outer/incomplete context and mon remain pending and are not added to the input dictionaries.

Round 31 completes the semantic unit-completion rules: propagate constraints from surrounding expressions, retain candidates that incomplete context cannot exclude, and offer conversion targets for every still-valid interpretation without hidden type selection. Month notation is mon/мес. The former Latin spelling is removed rather than retained as an alias; the semantic key is unchanged. Active examples and the generated operator help use the replacement, including compact periods such as 1mon3d. Historical interview records preserve their original proposals with the later correction recorded explicitly.

Round 32 adds accepted interface settings and management/help labels with English metadata and ru/en. Unsupported interface languages resolve to English only, while the number-format locale remains independent; an unavailable individual message uses English source text. Keyboard and startup defaults are specified in [interface/settings.json](../interface/settings.json). The generic candidate copy-behavior controls are not activated by this round; standard copy behavior remains as previously accepted.

Round 33 accepts a right-hand result column in the extended sheet, a floating library over the sheet, and the shared light-sheet appearance. Library message locations reflect that geometry; strings and stable keys are unchanged. The [workspace contract](../interface/workspace.json) records the visual decision separately from unresolved native/accessibility verification.

Round 34 replaces visible library action labels with accessible plus/ellipsis icon buttons and puts all item actions in menus. Launch parameters use one label/input row, required markers and optional in-field examples; parameter errors remain accessible without a visible explanation row. Import/restore/unsaved titles and actions are shortened; the restore preview still identifies affected history and settings. Underlying backup and data policies are unchanged. Each added or revised message keeps English meaning/context/location/type metadata and en/ru values.

Round 35 accepts public direct installation, automatic or manual update checks, explicit installation with saved data and restored working state, and data preservation after uninstall. Nine update actions, statuses and setting labels have English source text with meaning/context/location/elementType and matching en/ru entries. See the [delivery policy](../distribution/policy.json). Package-owned installer strings will be catalogued after packaging is selected; operating-system-owned dialogs are not application translations. The accepted Windows clarification selects one x64 build for Windows 11; no separate x86 build is required. Performance budgets are accepted in round 36. The short complexity diagnostic and its general-help explanation have English metadata and matching en/ru text; no benchmark result is claimed.


Round 37 accepts the inclusive minimum nonzero working magnitude 10^(-1000). The number-too-small diagnostic and the range explanation have English metadata with en/ru values. Existing small-value help now explicitly applies only inside the working range; approximately-zero display still retains a nonzero working value.
