# Модель расчёта с переменными

Status: claimed
Assignee: Codex
Type: grilling
Label: wayfinder:grilling
Parent: ../map.md
Blocked by: 01, 04

## Question

Как устроен расширенный расчёт: область видимости и время жизни переменных, порядок вычислений, реакция на правку предыдущего выражения, отображение результатов, поведение при ошибке и переход между быстрым и расширенным режимами?

Сравнить изменение исходного значения, повторное присваивание, неизвестную переменную и зависимость между строками. Если выбран автоматический пересчёт, определить поведение циклов и ссылок вперёд. Согласовать, какие действия редактируют текущий расчёт, а какие создают новый, и как пользователь выбирает результат для копирования.

## Comments

- В вопросе [Язык выражений и правила вычисления](04-expression-language.md) для тела многострочной функции приняты последовательное вычисление строк и локальные параметры и промежуточные имена. Это правило тела функции; согласованная модель автоматического пересчёта листа сохраняется. Связи между строками самого листа предстоит определить в текущем вопросе.

### Дополнение пользователя — открытие истории

В раунде 16 [История, подтверждение расчёта и копирование](06-history-and-copy.md) пользователь назначил Alt + стрелка вверх, с любой стороны клавиатуры, для открытия истории из расширенного режима. Команда заменяет прежнее назначение Ctrl+Up для доступа к истории; сама по себе не утверждает выход из листа или его замену. Обычные стрелки при редактировании остаются текстовыми. Этот вопрос взят в частичную работу для следующего полного раунда; зависимость от окончательных правил языка сохраняется, вопрос целиком не разрешён.

### Раунд 17 — предложения: расширенный лист

Нумерация общая с вопросом о возврате к исходному вводу в [История, подтверждение расчёта и копирование](06-history-and-copy.md), где находится пункт 1. Пункты 2–6 ниже — независимые предложения; ответы на предыдущий раунд не являются их согласованием.

2. **История внутри расширенного режима.** По уже принятому Alt+Up раскрывать историю и переводить фокус в неё, сохраняя текст листа и позицию курсора/выделение. Вверх-вниз выбирают запись без изменения листа, влево-вправо — её сохранённый ответ; начальный выбор — последняя запись и её ответ, выбранный при подтверждении. При переходе к другой записи выбран её подтверждённый ответ; стрелки останавливаются на краях, выбранный элемент прокручивается в видимую область. Ctrl+C / Cmd+C копирует выбранный исторический ответ по существующему правилу без пересчёта и сохраняет фокус в истории. Enter закрывает историю и вставляет текст выбранной формулы в сохранённую позицию курсора вместо выделения как одну обычную вставку; лист пересчитывается, но подтверждение и новая запись истории не создаются. Escape или повторное Alt+Up закрывает историю без вставки и возвращает прежний фокус/курсор. При отсутствии записей нечего выбрать или вставить, лист сохраняется. Рекомендовано явное принятие вставки: пример, курсор после `итог = `, выбранная формула `2 + 2`, Enter даёт `итог = 2 + 2`. Это отдельный контекст открытой истории; обычные стрелки в редакторе и подсказки сохраняют свои правила. Размещение истории и доступность проверяются прототипом, не считаются уже проверенными.

3. **Модель переменных листа.** Рекомендовано определять каждое локальное имя в листе один раз и пересчитывать по зависимостям, разрешая ссылки на определения ниже по тексту. Например, `сумма = цена * количество` может стоять перед `цена = 100` и `количество = 3` и даёт 300. Второе определение того же локального имени — понятная ошибка; для изменения цены редактируют исходное определение. Циклическая зависимость — ошибка участвующих определений и зависимых от них строк; независимые строки продолжают считаться. Это единая предлагаемая модель листа. Она не изменяет уже принятое последовательное исполнение тела многострочной функции. Реализация графа и точные диагностики уточняются технически после выбора модели.

4. **Результаты строк.** Рекомендовано показывать собственный результат у каждой завершённой строки с выражением или присваиванием: `цена = 100` имеет результат 100, `цена * 3` — 300. Пустые строки и комментарии не имеют результата. Если строка даёт несколько допустимых ответов, показывать весь набор по уже принятым правилам группировки, порядка и горизонтальной прокрутки. Это выбирает наличие результата на каждой вычисляемой строке, не только последнего итога; расположение, оформление и способы копирования из листа — последующая проверка интерфейса. Предложение независимо от разрешения ссылок вперёд и повторных присваиваний.

5. **Локальные имена и сохранённые определения.** Рекомендовано ограничивать переменные конкретным листом. В других листах и быстром режиме они не появляются как глобальные определения. Локальная переменная имеет приоритет над одноимённой пользовательской сохранённой константой в этом листе; сама константа не меняется. Например, глобальная `цена = 50`, в листе локальная `цена = 100`: здесь используется 100, в быстром вводе остаётся 50. Встроенные защищённые имена сохраняют прежнюю защиту. Конфликты с именами пользовательских функций и управление глобальными определениями остаются связанным вопросом [Пользовательские константы и функции](13-user-definitions.md), не определяются примером совпадения с константой.

6. **Сохранение и повторное открытие листов.** Рекомендованы несколько именованных листов с локальным автосохранением текста, включая незавершённые и ошибочные правки; история расчётов при автосохранении не пополняется. При повторном открытии восстанавливается текст листа и выполняется новый расчёт с текущими сохранёнными константами и функциями; прошлые показанные ответы не выдаются за результат нового расчёта. Лист остаётся изменяемым расчётом, а снимки подтверждённых ответов относятся к истории. Сохранение шаблонов остаётся отдельным действием. Меню листов, создание/переименование/удаление, перенос и восстановление после сбоя уточняются в [Шаблоны, сохранение и перенос данных](07-templates-and-data.md). Этот пункт выбирает пользовательскую модель сохранения, а не формат файла или алгоритм записи.

### Раунд 17 — ответы пользователя на пункты 2–3

Пользователь принял пункты 1–3 («ok»); пункт 1 о коротком режиме записан в [История, подтверждение расчёта и копирование](06-history-and-copy.md). По пунктам 4–6 пользователь уточнил: относятся ли они только к расширенному листу и не меняют ли движок пользовательских констант и функций. Это вопрос об области действия, не согласование оставшихся предложений.

2. Принято: Alt+Up с любым Alt открывает историю в расширенном режиме и переводит фокус в неё, сохраняя лист и курсор/выделение. Вверх-вниз выбирают запись, влево-вправо — её сохранённый ответ; лист от навигации не меняется. Начальный выбор — последняя запись и её подтверждённый ответ; при переходе к другой записи выбирается её подтверждённый ответ. На краях стрелки останавливаются, выбранный элемент прокручивается в видимую область. Ctrl+C / Cmd+C копирует выбранный исторический ответ без пересчёта по общим правилам буфера, сохраняя фокус в истории. Enter закрывает историю и вставляет формулу в сохранённую позицию курсора вместо выделения одним обычным действием вставки; затем лист пересчитывается без подтверждения и добавления записи. Escape или повторное Alt+Up закрывает историю без вставки и возвращает прежние фокус/курсор. При пустой истории лист сохраняется, выбора и вставки нет. Обычные стрелки редактора и подсказки сохраняют собственные контексты. Проверка размещения, фокуса и доступности ещё не выполнена.
3. Принята модель зависимостей листа: каждое локальное имя задаётся один раз, ссылки на определения ниже разрешены. Изменение исходного определения пересчитывает зависимые выражения. Повторное определение имени — ошибка, не последовательная замена его значения. Цикл делает невычислимыми участвующие определения и зависимые от них строки; независимые строки продолжают считаться. Например, строка `сумма = цена * количество` перед `цена = 100` и `количество = 3` даёт 300. Это семантика листа; тело многострочной пользовательской функции сохраняет уже принятое последовательное выполнение. Конкретный алгоритм и формулировки диагностик являются техническими/редакционными решениями, не новыми предпочтениями пользователя.

### Раунд 17 — уточнение области пунктов 4–6 перед ответом

- Пункт 4 касается показа результатов строк расширенного листа. Он не вводит показ результата каждой строки внутри пользовательской функции и не меняет возвращаемое функцией последнее выражение.
- Пункт 5 касается области локальных переменных листа и их взаимодействия с глобальной библиотекой. Предложенное перекрытие одноимённой сохранённой константы действует только при вычислении этого листа и не изменяет саму константу. Это предложение всё ещё ожидает ответа. Общие правила разрешения имён пользовательских функций, редактирования и зависимостей глобальных определений остаются отдельным вопросом.
- Пункт 6 касается хранения именованных листов как документов и их автосохранения. Он не вводит автосохранение глобальных констант/функций и не отменяет принятое явное сохранение пользовательских определений.

Общее вычислительное ядро сохраняется: различаются контексты листа, тела функции и библиотеки сохранённых определений, а не независимо реализованные математические движки. Никакое предложение пунктов 4–6 не перенесено в согласованные требования или пользовательскую справку до ответа пользователя. Следующий раунд пока не открыт.

### Раунд 17 — ответы пользователя на пункты 4–6

После уточнения области пользователь ответил «4-6 ok». Пункты 4–6 приняты только для расширенного листа и его взаимодействия с библиотекой; ранее принятые пункты 1–3 сохраняются. Весь раунд 17 согласован, но вопрос модели листа целиком ещё открыт.

4. У каждой завершённой строки с выражением или присваиванием показывается её результат: `цена = 100` — 100, `цена * 3` — 300. Пустые строки и комментарии не имеют результата. При нескольких допустимых ответах показывается весь набор по принятым правилам группировки, порядка и горизонтальной прокрутки. Точное размещение, оформление и способы копирования из листа требуют следующего решения/прототипа. Это не показ всех промежуточных строк внутри тела пользовательской функции.
5. Переменные относятся только к конкретному листу и не становятся глобальными определениями для быстрого ввода или других листов. Локальная переменная имеет приоритет над одноимённой сохранённой константой внутри этого листа, не меняя глобальную константу. При глобальной `цена = 50` и локальной `цена = 100` лист использует 100, быстрый ввод — 50. Встроенные имена остаются защищены. Разрешение имён внутри вызванной сохранённой функции и конфликты с именами функций требуют отдельного правила; они не выбраны одним примером перекрытия константы.
6. Приняты несколько именованных листов с локальным автосохранением текста, включая незавершённые и ошибочные правки. Автосохранение не пополняет историю. При повторном открытии восстанавливается текст, затем выполняется новый расчёт с текущими сохранёнными константами и функциями. Лист — изменяемый расчёт; история хранит подтверждённые ответы. Сохранение шаблона остаётся отдельным действием. Глобальные пользовательские определения сохраняются прежним явным действием, автосохранение листа его не выполняет. Команды управления листами, перенос и восстановление при сбое ещё уточняются.

Общее вычислительное ядро обслуживает эти разные контексты; отдельный математический движок для листа не вводится. Принятые правила отражены в сводной спецификации и справке ru/en, проверки интерфейса ещё не выполнены.

### Раунд 18 — предложения: завершение поведения листа

Общий раунд с [Пользовательские константы и функции](13-user-definitions.md), где находятся пункты 3–8. При постановке раунда все восемь пунктов были предложениями; итоговое согласование записано ниже.

1. **Клавиши, копирование и история листа.** Рекомендован единый сценарий: Enter в редакторе добавляет строку. Ctrl+C / Cmd+C при выделении копирует текст, без выделения — единственный готовый ответ вычисляемой строки с курсором; при нескольких ответах показывает прежнюю подсказку «Выберите результат». Клик копирует конкретный ответ строки. Ctrl+Enter на Windows / Cmd+Enter на macOS выполняет копирование ответа строки: если он один, копирует сразу, если несколько — переводит фокус в её существующий ряд для выбора влево-вправо. В ряду Enter или модифицированный Enter копирует выбранный ответ и возвращает прежний фокус/курсор редактора; Ctrl+C / Cmd+C копирует выбранный ответ с сохранением фокуса в ряду, Escape возвращает курсор без копирования. Набор/вставка/Backspace/Delete из ряда выполняют обычную правку в прежней позиции, прекращая выбор. Не копировать отсутствующий, ошибочный или устаревший ответ. Применяются общие правила числового буфера без единиц и локализованного логического текста. Лист при копировании остаётся открытым и не очищается. Автосохранение, вычисления и эти команды копирования не добавляют лист или строку в общую историю; предложено сохранять историю для подтверждений быстрого режима, а листы — как отдельные документы. Контекст истории, открытой по Alt+Up, и её Enter для вставки остаются отдельным уже принятым режимом. Это цельное предложение о клавишах и фиксации результатов листа; оно не переносит сюда скрытие окна или сброс Undo быстрого режима.

2. **Ошибки в листе.** Рекомендовано при ошибке или незавершённости выражения убирать его готовый ответ и не давать готовых ответов зависимым выражениям, сохраняя вычисление независимых частей. Пример: `a = 1 / 0`, `b = a + 1`, `c = 2 + 2`: a показывает ошибку, b — невозможность получить зависимость, c даёт 4. При неизвестном имени — указание на имя, при незавершённом вводе — отсутствие готового результата по прежнему правилу; предыдущие значения не выдаются за актуальные. Ошибочный текст продолжает автосохраняться. Уже принятое ленивое выполнение if/and/or сохраняется: неиспользуемая ветка не вычисляется. Точное восстановление границ выражений при синтаксической ошибке и алгоритм пересчёта являются технической работой; предложение задаёт поведение для различимых выражений и их зависимостей.

### Раунд 18 — ответы пользователя на пункты 1–2

Пользователь ответил «ok» на все восемь пунктов. Пункты 3–8 приняты в [Пользовательские константы и функции](13-user-definitions.md). Ниже зафиксированы правила листа; вопрос целиком не закрыт, размещение результатов и нативное поведение ещё требуют проверки.

1. **Копирование в листе.** Enter в редакторе вставляет новую строку. Ctrl+C на Windows / Cmd+C на macOS с выделением копирует выделенный видимый текст; без выделения копирует единственный готовый отображаемый ответ вычисляемого выражения, содержащего курсор. Считаются все отображаемые ответы этого выражения, включая ушедшие за горизонтальную границу, а не результаты всего листа. Если ответов несколько, буфер сохраняется и показывается «Выберите результат». Клик копирует нажатый ответ. Ctrl+Enter / Cmd+Enter копирует единственный ответ текущего выражения; при нескольких переводит фокус в его ряд для выбора стрелками влево-вправо.

   При фокусе в ряду Enter или модифицированный Enter копирует выбранный ответ и возвращает прежний фокус/курсор редактора. Ctrl+C / Cmd+C копирует выбранный ответ, сохраняя фокус в ряду; неактивное выделение редактора не меняет цель команды. Escape возвращает редактор без копирования. Набор, вставка, Backspace и Delete завершают выбор и выполняют обычную правку на прежнем месте курсора/выделения. Отсутствующий, ошибочный или устаревший ответ не копируется. Числа копируются как показанная числовая часть без единиц, сохраняя округление, все показанные цифры, знак, группировку, префикс и регистр системы счисления; логические ответы — показанным локализованным текстом. Лист остаётся открытым и не очищается. Эти действия, пересчёт и автосохранение не добавляют лист или строку в общую историю: она хранит подтверждения быстрого режима. Копирование из листа не начинает новую цепочку текстовой отмены. Открытая через Alt+Up история сохраняет отдельное значение Enter — вставку исторической формулы. Остальные клавиши ряда, точное размещение и доступность не считаются согласованными или проверенными этим пунктом.

2. **Ошибки и неполный ввод.** Выражение с ошибкой или незавершённым вводом не имеет готового результата; зависящие от него выражения также не имеют готовых ответов. Независимые части продолжают считаться: `a = 1 / 0` показывает ошибку, `b = a + 1` — невозможность получить зависимость, `c = 2 + 2` — 4. Неизвестное имя указывается в диагностике. Для незавершённого ввода не придумывается математическая ошибка и не показывается прежнее значение как актуальное. Ошибочный текст автосохраняется. Неиспользуемые ветки `if` / `and` / `or` не вычисляются. Поведение задано для различимых выражений и зависимостей; восстановление границ после синтаксической ошибки и точный анализ циклов с условными ветками остаются работой по языку/архитектуре.
