# Размещение подсказок внутри и за пределами ввода

Status: claimed
Assignee: Codex
Type: prototype
Label: wayfinder:prototype
Parent: ../map.md
Blocked by: 03

## Question

Как показать варианты завершения следующими строками под текущим словом, совместив точное выравнивание букв, рамку, выход за границу ввода и описания вариантов в быстром и многострочном режимах?

Прямое требование пользователя: начало слова в каждом варианте находится ровно под началом уже введённого фрагмента; повторённые введённые буквы полупрозрачны, предлагаемое продолжение непрозрачно. Варианты обрамлены рамкой. Верхняя граница плашки может находиться внутри области ввода, нижняя — за её пределами. Прежнее предложение размещать меню под результатом заменено этим направлением.

Проверить коротким одноразовым прототипом общую рамку или отдельные рамки, расположение описаний, перекрытие последующих строк, поведение у границы экрана и точное выравнивание. Предел пяти вариантов ранее предлагался вместе с отвергнутым расположением и не считается отдельно подтверждённым.

## Comments

- Выделено из обсуждения [Язык выражений и правила вычисления](04-expression-language.md), раунд 12. В нём уже согласованы открытие с первой буквы, состав кандидатов, Tab для принятия, приоритет стрелок у открытого меню, закрытие меню первым Escape и вставка скобок функции с курсором внутри.
- Узкая визуальная проверка не требует завершения всех правил языка и жизненного цикла листов. Данные прототипа будут фиктивными; он не вычисляет формулы, не сохраняет пользовательские данные и не проверяет нативное окно ОС.
- Прототип сохранён в отдельной ветке `codex/prototype-inline-completion`, коммит `6f383bad1f58a8a07fe75200b566562d92b9ab04`, путь `.scratch/calculator/prototypes/inline-completion/index.html`; инструкция запуска и ограничения находятся в соседнем `README.md`. [Открытый локальный прототип](http://127.0.0.1:8768/?variant=A&lang=ru) требует работающего сервера; постоянный контекст — ветка, коммит и путь. Итоговое решение ещё не принято; проверка пользователем впереди.
- Агент осмотрел A/B/C в браузере и проверил выбор стрелкой, Tab, положение курсора фактической вставкой цифры в скобки, два Escape, быстрый и многострочный режимы, текст под курсором, обе темы, языки ru/en и масштаб текста 100/125/150%. Замеченное перекрытие последующих строк показано пользователю как предложение. Данные вымышлены; вычисления, история и нативное скрытие не реализованы. Структурные проверки переводов прототипа выполнены.

### Раунд 1 — ожидаются ответы

В прототипе сохранено заданное выравнивание во всех вариантах. Предложения, ещё не утверждённые пользователем:

1. Одна общая рамка вокруг всего списка подсказок.
2. Вариант A: короткое описание каждого варианта справа в той же строке. Для сравнения: B показывает описание выбранного варианта под списком, C — в боковой панели.
3. При редактировании в середине многострочного текста плашка временно перекрывает следующие строки, не раздвигая текст. После закрытия строки снова полностью видны. Введённое слово и остальные строки не меняют своих координат из-за открытия подсказок.
4. До пяти видимых вариантов; остальные доступны прокруткой, выбранный стрелками вариант удерживается в видимой области. Фиктивный пример содержит три варианта и не проверяет этот предел.

Положение у краёв экрана, точная прозрачность, трактовка границы поля и границы нативного окна, а также доступность остаются открытыми. В этом раунде они не утверждаются молча.
